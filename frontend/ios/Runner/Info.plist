<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- The language used during development. -->
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>

    <!-- The name of the app as it appears on the home screen. -->
    <key>CFBundleDisplayName</key>
    <string>AutoGPT Flutter Client</string>

    <!-- The name of the executable file. -->
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>

    <!-- The bundle identifier for the app. -->
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>

    <!-- The version of the Info.plist file. -->
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>

    <!-- The name of the product archive file. -->
    <key>CFBundleName</key>
    <string>auto_gpt_flutter_client</string>

    <!-- The type of the product archive file. -->
    <key>CFBundlePackageType</key>
    <string>APPL</string>

    <!-- The marketing version of the app. -->
    <key>CFBundleShortVersionString</key>
    <string>$(FLUTTER_BUILD_NAME)</string>

    <!-- The build version of the app. -->
    <key>CFBundleVersion</key>
    <string>$(FLUTTER_BUILD_NUMBER)</string>

    <!-- Specifies that the app requires an iPhone OS. -->
    <key>LSRequiresIPhoneOS</key>
    <true/>

    <!-- The name of the launch storyboard file. -->
    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>

    <!-- The name of the main storyboard file. -->
    <key>UIMainStoryboardFile</key>
    <string>Main</string>

    <!-- The interface orientations supported by the app. -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- The interface orientations supported
