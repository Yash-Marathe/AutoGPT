# .envrc

if [[ -z "${IN_NIX_SHELL:-}" ]]; then
  if command -v direnv >/dev/null 2>&1 && [[ "$(direnv query auto_gpt)" == "allowed" ]]; then
    eval "$(direnv printenv auto_gpt)"
  else
    echo "AutoGPT is not allowed by direnv. Please allow it using 'direnv allow'."
  fi
fi

